# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-ssr/
build/
out/

# Vite / Wrangler / workerd caches
.vite/
.wrangler/
.dev.vars

# Alchemy state
.alchemy/

# Prisma generated clients + local dev DB
prisma/generated/
prisma/dev.db
prisma/dev.db-journal

# Live generated (committed if you want, but keep generated noise out)
# prisma/migrations/*.sql  ← comment to commit SQL migrations

# Bun
bun.lockb

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env files (keep .env.example tracked)
.env
.env.local
.env.*.local

# Test artifacts
test-results/
playwright-report/
playwright/.cache/

# TypeScript build info
*.tsbuildinfo

# Local override files (lockfile or peer-dep pinning)
# bun.lock  ← bun-lockfile is committed for reproducibility

# Session / chat logs that shouldn't land in the repo
*.md.local
session-*.md
references/
