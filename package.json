{
  "name": "livestore-tanstack-db-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "type": "module",
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build",
    "dev:spa": "cd examples/spa && bun run dev",
    "dev:start-orpc": "cd examples/tanstack-start-orpc && bun run dev",
    "deploy:spa": "cd examples/spa && bun alchemy deploy",
    "deploy:start-orpc": "cd examples/tanstack-start-orpc && bun alchemy deploy",
    "gen": "bunx prisma generate --schema=./examples/spa/prisma/schema.prisma && bunx prisma generate --schema=./examples/tanstack-start-orpc/prisma/schema.prisma && bun run build",
    "gen:spa": "bunx prisma generate --schema=./examples/spa/prisma/schema.prisma",
    "gen:start-orpc": "bunx prisma generate --schema=./examples/tanstack-start-orpc/prisma/schema.prisma"
  },
  "overrides": {
    "effect": "4.0.0-beta.98"
  },
  "dependencies": {
    "@distilled.cloud/cloudflare-rolldown-plugin": "^0.13.7",
    "@distilled.cloud/cloudflare-runtime": "^0.13.7",
    "@distilled.cloud/cloudflare-vite-plugin": "^0.13.7"
  }
}
