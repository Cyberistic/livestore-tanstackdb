# Implementation Plans

Generated by the improve skill on 2026-07-14. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Planned against commit `f2cd0dbd`. The working tree had uncommitted changes at planning time; executors should run the drift check in each plan before proceeding.

## Execution order & status

| Plan | Title                                                                    | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------------ | -------- | ------ | ---------- | ------ |
| 001 | Add unit tests to integration packages | P1 | M | — | DONE |
| 002 | Fix client-document insert event in `useTable` | P1 | S | 001 | TODO |
| 004 | Fix delete handlers to use the configured soft-delete column | P1 | S | 001 | TODO |
| 005 | Memoize `useLiveStore` return value | P2 | S | 001 | TODO |
| 006 | Deduplicate shared schema helpers and types | P2 | M | 001 | DONE |
| 007 | Remove unused React dependencies from `livestore-prisma` | P2 | S | — | TODO |
| 008 | Move `@prisma/generator-helper` to `livestore-prisma` dependencies | P1 | S | — | TODO |
| 009 | Replace `require()` with dynamic `import()` in `LiveStoreDevtoolsBridge` | P2 | S | 001 | DONE |
| 011 | Update transitive vulnerable dependencies | P2 | M | — | TODO |
| 012 | Clean stale build artifacts in `livestore-prisma/dist` | P2 | S | — | TODO |
| 013 | Align TypeScript versions across the workspace | P3 | S | — | TODO |
| 014 | Tighten the `effect` peer dependency range | P3 | S | — | TODO |
| 015 | Validate or fallback for unknown Prisma column types | P2 | S | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002, 004, 005, 006, 009, 015** depend on **001** because they add or modify code that should be covered by the new unit-test baseline. Run the package test suites before and after these changes.
- **006** is recommended before **004** because deduplicating the shared helpers first makes the soft-delete column resolution easier to centralize. However, they are not strictly ordered.
- **007** and **008** are independent packaging fixes that can land in any order. If both are executed, run `bun install` after each.
- **011** should be followed by a full `bun run typecheck` and `bun run gen` because dependency updates can affect the examples.
- **012** should be run before any verification that reads `packages/livestore-prisma/dist` to ensure the directory reflects current source files.
- **013** may expose type errors hidden by the TypeScript 6.0.3 preview; if so, it should be completed before other source changes that might rely on the newer compiler behavior.

## Findings considered and rejected

- **003 — Move example sync auth token to environment variables**: rejected at the user's request because the example is intentionally a demo and the placeholder token is not a real secret. If the examples are promoted beyond demo use, this plan should be reconsidered.
- **010 — Fix Cloudflare env/D1 bindings stored in global/module-level state**: rejected at the user's request because the example is intentionally a demo and not a production deployment target. If the examples are used as a production template, this plan should be reconsidered.
